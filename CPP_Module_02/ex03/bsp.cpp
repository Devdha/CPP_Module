#include "Point.hpp"
#include "Fixed.hpp"

bool bsp(Point const a, Point const b, Point const c, Point const point) {
    int     cnt = 0;
    float   isX;

    // if (a.getX())
    return cnt % 2 == 1;
}